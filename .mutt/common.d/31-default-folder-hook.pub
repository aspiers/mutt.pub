# All matching hooks are executed in the order they appear, so this
# default needs to come before folder-specific ones but after
# mutt/.mutt/common.d/30-default-folder-hook
folder-hook . source ~/.mutt/folders/DEFAULTS.pub
