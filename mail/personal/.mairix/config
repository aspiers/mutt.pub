base=~/mail/personal
# N.B. Can also use $FOO or ${FOO} here

maildir=in...
maildir=offlineimap...
maildir=inboxes...
maildir=archive...
maildir=drafts...
mbox=archive/OW*
mbox=inboxes/OLD-MBOXES/*
omit=in/raw.*
omit=inboxes/OLD-MBOXES/me
omit="offlineimap/[Google Mail].All Mail"
# I don't understand; this seems to work for Spam but not Bin:
omit="offlineimap/[Google Mail].Spam"
omit="offlineimap/[Google Mail].Bin"

# Where does the results folder get created? (relative to base)
mfolder=mairix

# What format should the results folder be?
mformat=maildir

# Where does the index database live?
database=$MAIRIX_DIR/index-$MAIRIX_VERSION
